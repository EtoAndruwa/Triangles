#include "triangles.h"

int main(int argc, char* argv[])
{
    // #ifdef DEBUG
        
    // #endif

    // point p;
    // p.print_point();

    // point p1(1.5,2,3);
    // p1.print_point();

    vector v1(1, 2, 3, 5, 6, 7);
    v1.print_vector();
    
 
    return 0;
}

